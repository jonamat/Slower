/** dB floor of the 8-bit spectrogram quantisation: byte 0 == DB_FLOOR, byte 255 == 0 dBFS. */
export const DB_FLOOR = -110;
/** Spectrogram tile width in columns (one WebGL texture per tile). */
export const TILE_W = 2048;
